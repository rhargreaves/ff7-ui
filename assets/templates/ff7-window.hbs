{{#if character}}
<h1>{{character}}</h1>
{{/if}}
{{#if text}}
<p>{{breaklines text}}</p>
{{/if}}
{{#if options}}
<ul>
	{{#each options}}
	<li {{bind-attr data-test=text}}>{{breaklines text}}</li>
	{{/each}}
</ul>
{{/if}}
